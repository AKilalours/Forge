"""CUDA kernels written for this project, and the reference implementations they replace.

Nothing here is imported at package import time. The extension compiles on first use, so a
machine with no CUDA toolchain can import forge and run everything else.
"""
